<launch>
  <!-- Assume the rosbridge_server, tf2_web_republisher, Collada server, and ros_web_video are already running. -->
  <!-- Interactive marker proxy is needed per topic. -->
  <!-- See https://github.com/jstnhuang/ros-rviz/wiki/User-guide -->
</launch>
